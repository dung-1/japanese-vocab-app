import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dungcts.japanese.app',
  appName: 'japanese-vocab-app',
  webDir: 'dist/japanese-vocab-app/browser',
};

export default config;
